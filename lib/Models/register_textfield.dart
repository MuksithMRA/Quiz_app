import 'package:flutter/cupertino.dart';

class RegisterTextField {
  IconData icon;
  String label;
  bool isPassword;

  RegisterTextField(
      {required this.icon, required this.label, required this.isPassword});
}
